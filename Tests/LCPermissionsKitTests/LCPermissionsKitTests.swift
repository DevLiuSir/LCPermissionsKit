import XCTest
@testable import LCPermissionsKit

final class LCPermissionsKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LCPermissionsKit().text, "Hello, World!")
    }
}
